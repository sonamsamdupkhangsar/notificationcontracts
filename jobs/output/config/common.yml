default:
  app:
    name: Job API
    version: 1.0.0

  broker:
    mqtt:
      url: mqtt://test.mosquitto.org
      topics: ["job/posted"]
      qos:
      protocol: mqtt
      retain:
      subscribe: true

development:

test:

staging:

production:
