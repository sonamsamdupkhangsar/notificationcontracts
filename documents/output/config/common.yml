default:
  app:
    name: Document API
    version: 1.0.0

  broker:
    mqtt:
      url: mqtt://test.mosquitto.org
      topics: ["document/requested"]
      qos:
      protocol: mqtt
      retain:
      subscribe: true

development:

test:

staging:

production:
